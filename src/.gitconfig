[user]
    name = Mike Fisher
    email = mike.fisher@skyscanner.net
	signingkey = FAD5367B1AAE64CD
[core]
	excludesfile = /Users/mbfisher/.gitignore
[alias]
    lg = log --graph --pretty=format:'%C(dim)%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --no-merges

    a = add --all .
    c = commit
    cm = commit -m
    ca = commit --amend
    cb = checkout -b
    d = diff
    ds = diff --staged
    s = status
    ps = push -u
    pf = push -f
    pt = push --tags
    pl = pull
    pr = pull --rebase
	diff = diff --color
    add = add --all .
    squash = "! git-squash"
    as = "! git add -u . && git s"

[push]
	default = current
[pull]
    default = current
[rerere]
	enabled = true
	autoupdate = true
#[commit]
#   gpgSign = true


[url "git@gitlab.com:"]
	insteadOf = https://gitlab.com/
